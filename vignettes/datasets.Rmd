---
title: "Explore datasets"
date: "`r Sys.Date()`"
output: rmarkdown::html_vignette
header-includes:
  - \usepackage[utf8]{inputenc}
vignette: >
  %\VignetteIndexEntry{Explore datasets}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r, include = FALSE}
knitr::opts_chunk$set(
  tidy = "styler",
  collapse = TRUE,
  comment = "#>"
)
```

```{r setup, include = FALSE}
library(dplyr)
library(DT)
if(!require(rjavacmecab)){
  devtools::load_all()
}
```

## AozoraBunkoSnapshot

```{r}
data("AozoraBunkoSnapshot")
snapshot <- dplyr::sample_n(AozoraBunkoSnapshot, 20) # Because it is too large to show whole entries, sampling 20 rows.
DT::datatable(snapshot)
```

## MiyazawaKenji

```{r}
data("MiyazawaKenji")
DT::datatable(MiyazawaKenji)
```

## ExtendedLettersJp

```{r}
data("ExtendedLettersJp")
DT::datatable(ExtendedLettersJp)
```

## OneLettersJp

```{r}
data("OneLettersJp")
DT::datatable(OneLettersJp)
```

## StopWordsJp

```{r}
data("StopWordsJp")
DT::datatable(StopWordsJp)
```

